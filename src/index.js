import React from 'react';
import ReactDOM from 'react-dom';

const baseURL = "https//api.github.com/users/";

function App() {
    return (
        <div>
            app;
        </div>
    )
}

const rootNode = document.getElementsByID("root");
ReactDOM.render(<App />, rootNode);